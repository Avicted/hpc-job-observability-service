# oapi-codegen configuration for server stubs
# This generates the server interface that handlers must implement
# Run: go generate ./...

package: server
output: server.gen.go
generate:
  echo-server: true
  strict-server: true
  models: true
  embedded-spec: true
output-options:
  skip-prune: true
